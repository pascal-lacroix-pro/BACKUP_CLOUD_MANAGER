# Copier ce fichier en .env et remplir les vraies valeurs.
# Ne jamais committer .env dans git.

# Chemin absolu vers la racine du SSD externe (process main Electron uniquement)
SSD_PATH=/Volumes/MonSSD/Backups

# Nom du remote rclone configuré pour Dropbox (rclone config)
DROPBOX_PATH=dropbox
